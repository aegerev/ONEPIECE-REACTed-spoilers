import '../styles/Header.css';

function Header () {
    // TODO: Add a style attribute to header and the h1 element

    return (
        <header className="Header">
            <h1> Welcome to the One Piece Spoiler Site!</h1>
        </header>
    );
}

export default Header;